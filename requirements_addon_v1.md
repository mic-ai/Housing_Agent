# HomeReelMatch 追加要件定義書 — 購入検討者向け段階的知識コンテンツ & 営業担当プロフィール強化

**ドキュメントバージョン:** v1.0
**作成日:** 2026-07-08
**対象:** 既存 `requirements.md`（v1.0）への追加機能
**プロジェクトコード名:** HomeReelMatch

---

## 0. 背景と設計方針

既存の HomeReelMatch は「住宅情報動画を見る → 紹介している営業マンに好感を持つ → コンタクトする」という、コンテンツ起点の接触フローを前提としている。本追加要件は、このフローを変更せず、**その手前に「知識ゼロのユーザーが安心して検討を進められる段階的な学習体験」を追加する**ものである。

### 0.1 設計原則（本要件全体に適用）

| 原則 | 内容 |
|------|------|
| フローを変えない | 「動画視聴 → 好感 → コンタクト」の既存フローは変更しない。学習コンテンツはその前段階の導線として追加する |
| 押し付けない | マッチングアルゴリズム・相性スコア・営業担当のランキング/おすすめ順は実装しない。並び順は常に「新しい順」「保存順」など機械的な基準のみ |
| 記事のみで完結 | 学習コンテンツは記事（テキスト）のみで構成する。新規動画の制作は行わない。既存のメーカー特徴動画（本編動画）は③の段階で別途閲覧するのみとし、学習記事に無理に埋め込まない |
| CMSで運用可能 | 記事コンテンツは管理者がダッシュボードから作成・編集・公開できる。コードへのハードコードは行わない |
| 匿名前提を維持 | 既存の閲覧ユーザーはログイン不要。進捗・保存情報も、アカウント登録なしで機能する軽量な識別方式（0.2節）を用いる |

### 0.2 前提機能：閲覧ユーザーの軽量識別（新規）

進捗記録・保存済みメーカー・閲覧済み営業マンの記録には、ユーザーを跨いだセッションの識別が必要になる。既存の `User` モデルはコンタクト申請者の記録用であり、閲覧中の匿名ユーザーを識別する仕組みがないため、以下を新設する。

- ブラウザに `viewerToken`（UUID）を Cookie（有効期限1年、`httpOnly: false`）として発行する
- 初回アクセス時に `POST /api/viewer/init` を呼び、`ViewerProfile` レコードを作成し `viewerToken` を返す
- ログイン・会員登録は要求しない。将来的にコンタクト申請時の `User` と `ViewerProfile` を紐づける拡張は本要件のスコープ外とする

---

## 1. 機能A：営業担当プロフィール「人となり」強化

### 1.1 目的

現行の `/salesperson/[salespersonId]` ページ（ヒーローカード＋簡易プロフィール／`profileDetail`／コンタクトカードの3カード構成）は、名前・会社・経歴程度の情報にとどまる。動画で好感を持ったユーザーが「この人になら相談できそう」と確信するための人柄の情報を追加する。

### 1.2 追加要素

| 要素 | 内容 | 入力元 |
|------|------|--------|
| 自己紹介動画 | 既存の `SalespersonProfileVideo` を流用。プロフィールページ上部に大きく表示 | 営業マンダッシュボード（既存機能） |
| 大切にしていること | 「家づくりで大切にしていること」を営業マン本人の言葉で記述（300字程度） | 新規：`Salesperson.valuesStatement` |
| スタンス一言 | ヒーローカードに表示する短い引用文（60字程度） | 新規：`Salesperson.toneQuote` |
| 一問一答インタビュー | 質問（自由入力）＋回答（テキスト300字＋任意で30秒程度の動画クリップURL） | 新規：`SalespersonQAItem`（複数登録可） |
| 経歴サマリー | 経験年数・引き渡し棟数・得意分野タグ | 新規：`Salesperson.yearsExperience` / `handoverCount` / 既存ハッシュタグ流用 |
| この担当が紹介した住宅 | 既存の `videoSegments`（担当動画一覧）を流用 | 既存機能 |

### 1.3 画面要件

**P-07: 営業担当プロフィール（拡張）** — `/salesperson/[salespersonId]`（既存パス、内容を拡張）

- 自己紹介動画（大サムネイル・タップで再生）
- スタンス一言（引用ボックス）
- 「家づくりで大切にしていること」セクション
- 一問一答セクション（3〜6件、動画付きの回答にはアイコン表示）
- この担当が紹介した住宅（既存のvideoSegmentsグリッド、最大6件＋「すべて見る」）
- 経歴サマリー（経験年数／引き渡し棟数／得意分野）
- コンタクトCTA（既存のLINE/メールボタンを最下部に維持）

### 1.4 入力画面（営業マンダッシュボード拡張）

**S-xx: プロフィール編集（拡張）** — `/dashboard/profile`（既存パス）

- 既存の `profileDetail` 入力欄に加え、以下を追加：
  - 大切にしていること（テキストエリア、300字）
  - スタンス一言（テキスト、60字）
  - 経験年数／引き渡し棟数（数値）
  - 一問一答リスト（質問・回答テキスト・回答動画URLを行単位で追加/編集/削除、並び替え可）

### 1.5 非機能要件

- 一問一答の回答動画は新規アップロード機構を作らず、既存の `SalespersonProfileVideo` と同じ「外部URL登録方式（YouTube等）」を流用する（Supabase Storageへの新規アップロード種別は追加しない）
- 一問一答は最大10件までとする（プロフィールページの冗長化防止）

---

## 2. 機能B：段階的知識学習コンテンツ（CMS管理の記事のみ）

### 2.1 ユーザーフロー（4段階）

```
① 情報初心者向け記事
   何から考えたらいいか（立地・相場価格・ローンの借り方等）
        │
② メーカー選定向け記事
   メーカーをどう選ぶか（ざっくりとしたメーカー比較表）
        │  ← 比較表の行/メーカー名タップでフィルター条件を引き継ぐ（3章）
③ メーカー特徴動画閲覧
   既存の動画ポータル（検索・絞り込み）にフィルター付きで遷移
        │
④ 営業担当へのコンタクト（既存フロー・変更なし）
```

①②は本要件で新設する記事コンテンツ、③は既存の動画ポータル機能（`/`、`/tag/[tagName]` 等）をフィルター付きで呼び出すのみで新規開発は最小限、④は既存機能そのままである。

### 2.2 データモデル（CMS管理）

```
LearningPhase   — 学習フェーズ（① 情報収集 / ② メーカー選定 の2フェーズを初期データとして投入。将来フェーズ追加も可能な設計とする）
  - key, title, order, description, isActive

Article         — 記事（管理者がCMSから作成・編集・公開）
  - phaseId, order, title, bodyMarkdown, estimatedMinutes, difficulty
  - translateBoxLabel / translateBoxValue（「つまりあなたにとっては」翻訳ボックス、任意）
  - status: DRAFT | PUBLISHED
  - publishedAt

ArticleComparisonRow — ②の記事に付随する「ざっくり比較表」の行（任意・②の記事のみで使用）
  - articleId, houseMakerId（任意）, priceRangeTag, featureTag, order

ViewerArticleProgress — 閲覧ユーザーの記事既読状態
  - viewerId, articleId, completedAt
```

### 2.3 CMS要件（管理者ダッシュボード拡張）

**A-xx: 学習コンテンツ管理**（管理者ダッシュボードに新規タブ追加）

| 機能 | 内容 |
|------|------|
| フェーズ一覧 | フェーズの並び替え・有効/無効切り替え |
| 記事一覧 | フェーズ内の記事を`order`順に一覧表示。公開/下書きステータス表示 |
| 記事作成・編集 | タイトル・本文（Markdown）・所要時間・難易度・翻訳ボックスを入力するフォーム |
| 比較表編集（②の記事のみ） | 行の追加/削除、ハウスメーカー選択（既存`HouseMaker`から）、価格帯タグ・特徴タグの入力 |
| 公開/下書き切り替え | `DRAFT`→`PUBLISHED`。公開後は閲覧ユーザーの学習パスに即時反映 |
| プレビュー | 公開前に閲覧ユーザー向け画面と同じレイアウトで確認できる |

- 記事作成・編集・削除は `ADMIN` ロールのみ許可する（`requireAdmin()` を使用）。既存のロール分離方針を踏襲する

### 2.4 閲覧ユーザー向け画面要件

**P-08: マイ家づくり（学習ロードマップハブ）** — `/journey`（新規）

- 縦タイムラインで①②の各フェーズを表示。完了フェーズは畳んで表示、現在フェーズは展開して「次の記事」への導線を表示
- 各フェーズの進捗（既読記事数／全体）を表示

**P-09: フェーズ内学習パス（記事ビューア）** — `/journey/[phaseKey]/[articleOrder]`（新規）

- 記事本文＋翻訳ボックス（存在する場合）を表示
- ②のフェーズでは、記事末尾に比較表（`ArticleComparisonRow`）を表示。行・メーカー名タップで3章の遷移仕様に従い動画ポータルへ遷移
- 「読み終えた（次へ）」ボタンで `ViewerArticleProgress` を記録し、次の記事（または次フェーズ）へ遷移
- 前の記事に戻る導線（進捗は保持）

### 2.5 非機能要件

- 記事本文は Markdown で保存し、閲覧側で安全にレンダリングする（XSS対策として許可タグを制限したサニタイズを行う。生HTMLの保存は許可しない）
- 記事の閲覧はログイン不要。`viewerToken` が未発行の場合は初回アクセス時に自動発行する

---

## 3. 機能C：②→③ 比較表からの動画フィルター連携

### 3.1 要件

②の記事に含まれる「ざっくり比較表」の行、またはメーカー名をタップすると、そのメーカー・価格帯タグ・特徴タグを引き継いだ状態で既存の動画ポータル（`/` または `/tag/[tagName]`）へ遷移する。

### 3.2 遷移仕様

```
記事内比較表の行タップ
  → houseMakerId / priceRangeTag / featureTag をクエリパラメータとして付与
  → 例: /?houseMakerId=xxxx&featureTag=高断熱
  → 遷移先の VideoFeedClient は既存の FilterBar 状態にこのクエリパラメータを反映して表示
```

- 既存の `FilterBar` はメーカー・会場の2軸のみ絞り込み可能なため、`featureTag` はハッシュタグ（既存の `Hashtag` / `VideoHashtag`）として扱い、既存のタグ絞り込み機構（`/tag/[tagName]`）を流用する。新規のフィルター軸（エリア・価格帯の構造化フィールド）は本要件のスコープ外とする
- 引き継いだ条件はフィルターチップとして表示し、ユーザーが個別に解除できること（既存のFilterBarのUIパターンを踏襲）

---

## 4. 機能D：コンタクト前ハブ画面

### 4.1 目的

「担当と面談」段階に進んだユーザーに対し、動画視聴で好感を持った営業マンへの導線を整理して提示する。マッチングや推薦は行わず、ユーザー自身の閲覧履歴・保存メーカーを並べ直すのみとする。

### 4.2 データモデル

```
ViewerSalespersonView — 閲覧ユーザー×営業マンの視聴履歴
  - viewerId, salespersonId, videoId（最後に見た動画）, viewCount, lastViewedAt

ViewerSavedMaker — 閲覧ユーザーが保存/検討中のハウスメーカー
  - viewerId, houseMakerId, createdAt
  （保存操作自体は別途③の動画ポータル画面に「気になるメーカーに追加」ボタンを新設する必要がある。本要件では ViewerSavedMaker の保存APIのみ定義し、UIへの保存ボタン追加は既存の動画ポータル改修として別途スコープ化する）
```

### 4.3 画面要件

**P-10: 担当に相談する（コンタクト前ハブ）** — `/consult`（新規）

| セクション | 内容 |
|------|------|
| ベネフィット説明 | 「相談は無料」「契約の約束ではない」等、相談のハードルを下げる案内文（CMS管理外、静的コンテンツ） |
| これまで見た営業担当 | `ViewerSalespersonView` を `lastViewedAt` 降順で一覧表示。「もう一度見る」で該当動画（`videoId`）へ遷移 |
| 気になるメーカーの他の担当 | `ViewerSavedMaker` に登録されたメーカーに所属する営業マンのうち、`ViewerSalespersonView` に記録のない（未視聴の）担当を一覧表示。並び順は新着順のみ（推薦ロジックなし） |

### 4.4 非機能要件

- 閲覧履歴・保存メーカーが1件もない場合、それぞれのセクションは空状態メッセージを表示し、動画ポータルへの導線を出す
- 推薦・スコアリング・並び替えアルゴリズムは実装しない（0.1節の設計原則に従う）

---

## 5. 画面一覧（追加分）

| 画面ID | 画面名 | パス | 認証 |
|--------|--------|------|------|
| P-07 | 営業担当プロフィール（拡張） | `/salesperson/[salespersonId]` | 不要（既存パスの拡張） |
| P-08 | マイ家づくり（学習ロードマップハブ） | `/journey` | 不要 |
| P-09 | フェーズ内学習パス（記事ビューア） | `/journey/[phaseKey]/[articleOrder]` | 不要 |
| P-10 | 担当に相談する（コンタクト前ハブ） | `/consult` | 不要 |
| A-07 | 学習コンテンツ管理（フェーズ・記事CRUD） | `/admin/dashboard`（新規タブ） | ADMIN |

---

## 6. スコープ外（本要件で対応しないこと）

- 営業担当・メーカーのマッチングアルゴリズム、相性スコア、レコメンド順位付け
- 学習コンテンツ用の新規動画制作（既存動画の流用のみ）
- 閲覧ユーザーのアカウント登録・ログイン機能
- エリア・価格帯の構造化フィルター軸の新設（既存のハッシュタグ運用で代替）
- 予約のキャンセル・リスケジュール機能、営業マン向けアナリティクス機能（別要件として扱う）

---

## 7. 実装優先順位（推奨）

| 優先度 | 機能 | 理由 |
|--------|------|------|
| 高 | 0.2 閲覧ユーザーの軽量識別 | B・Dの前提機能のため最優先 |
| 高 | 機能B（学習コンテンツ・CMS） | 提供価値の中核。①②の記事が無いと後続機能が成立しない |
| 中 | 機能A（営業担当プロフィール強化） | 独立性が高く並行実装可能。既存`profileDetail`基盤の延長 |
| 中 | 機能C（比較表→動画フィルター連携） | 機能Bに依存 |
| 低 | 機能D（コンタクト前ハブ） | 視聴履歴の蓄積が一定量必要になるため、Bのリリース後が望ましい |
